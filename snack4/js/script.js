// scrivere una funzione trovaIndice() che, dati un array e un elemento in input, 
// restituisca la posizione in cui si trova l'elemento all'interno dell'array;
//  se l'elemento non è presente nell'array, allora la funzione deve restituire -1


// prendo un array e un elemento da cercare
    //prendo 1 a 1 gli elementi dell'array
        //se l'elemento da cercare è uguale a questo elemento dell'array
            //return dell'indice

    //return -1

const array = ["pippo", "pluto", "paperino", "walter", 3, 2, 1];

const result = trovaIndice(array, "walter");
console.log(result);